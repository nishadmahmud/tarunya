export const metadata = {
    title: 'Track Your Order | Pochondo Shop',
    description: 'Track the real-time status of your order at Pochondo Shop using your invoice ID.',
};

export default function TrackOrderLayout({ children }) {
    return <>{children}</>;
}
