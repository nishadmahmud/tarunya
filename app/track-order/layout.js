export const metadata = {
    title: 'Track Your Order | Tarunya Shop',
    description: 'Track the real-time status of your order at Tarunya Shop using your invoice ID.',
};

export default function TrackOrderLayout({ children }) {
    return <>{children}</>;
}
